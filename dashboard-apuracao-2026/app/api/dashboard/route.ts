import {NextResponse} from "next/server";
function parseCSV(text:string){
 const split=(l:string)=>l.split(/,(?=(?:[^"]*"[^"]*")*[^"]*$)/).map(v=>v.replace(/^"|"$/g,"").trim()),all=text.replace(/^\uFEFF/,"").trim().split(/\r?\n/).map(split),headerRow=all.findIndex(r=>r.some(v=>v.toLocaleLowerCase("pt-BR").includes("seção"))),rawHeader=headerRow>=0?all[headerRow]:all[0]||[],rows=all.slice((headerRow>=0?headerRow:0)+1),header=rawHeader.map(v=>v.toLocaleLowerCase("pt-BR")),find=(...n:string[])=>header.findIndex(h=>n.some(x=>h.includes(x))),code=find("cod. ibge","cod ibge"),section=find("seção","secao"),location=find("local"),city=find("município","municipio","cidade"),eligible=find("aptos"),candidate=find("sergio aguiar","sérgio aguiar"),federal=find("roger"),checked=find("check","verificação","verificacao");
 const data=rows.filter(r=>section>=0&&r[section]&&!r[section].toLocaleLowerCase("pt-BR").includes("total")).map(r=>({ibgeCode:code>=0?r[code]:"",section:r[section],location:location>=0?r[location]||"—":"—",municipality:city>=0?r[city]||"Camocim":"Camocim",eligible:eligible>=0?Number((r[eligible]||"0").replace(/\D/g,""))||0:0,votes:candidate>=0?Number((r[candidate]||"0").replace(/\./g,"").replace(",","."))||0:0,federalVotes:federal>=0?Number((r[federal]||"0").replace(/\./g,"").replace(",","."))||0:0,checked:checked>=0?/^(true|sim|x|1)$/i.test(r[checked]||""):false}));
 return {rows:data,federalCandidate:federal>=0?rawHeader[federal].trim()||"Roger":"Roger"};
}
export async function GET(){const url=process.env.SHEET_CSV_URL||"https://docs.google.com/spreadsheets/d/1LEDAYbRLuSSCVrUEnud_ONbY7-biZAzZ44ExAmmE_uc/export?format=csv&gid=1993398234";try{const r=await fetch(url,{cache:"no-store"});if(!r.ok)throw 0;const parsed=parseCSV(await r.text());return NextResponse.json({...parsed,source:"sheet",updatedAt:new Date().toISOString()})}catch{return NextResponse.json({rows:[],federalCandidate:"Roger",source:"demo",updatedAt:new Date().toISOString(),message:"Planilha indisponível. Compartilhe como: qualquer pessoa com o link — leitor."})}}
