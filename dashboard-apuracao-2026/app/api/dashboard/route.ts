import {NextResponse} from "next/server";
function parseCSV(text:string){const lines=text.replace(/^\uFEFF/,"").trim().split(/\r?\n/),split=(l:string)=>l.split(/,(?=(?:[^"]*"[^"]*")*[^"]*$)/).map(v=>v.replace(/^"|"$/g,"").trim()),rows=lines.map(split),header=(rows.shift()||[]).map(v=>v.toLocaleLowerCase("pt-BR")),find=(...n:string[])=>header.findIndex(h=>n.some(x=>h.includes(x))),code=find("cod. ibge","cod ibge"),section=find("seção","secao"),location=find("local"),city=find("município","municipio","cidade"),eligible=find("aptos"),candidate=header.findIndex(h=>h==="votos"||h.includes("votos sérgio")||h.includes("votos sergio")),votes=candidate>=0?candidate:Number(process.env.SHEET_VOTES_COLUMN_INDEX||"6");return rows.filter(r=>city>=0&&r[city]).map(r=>({ibgeCode:code>=0?r[code]:"",section:section>=0?r[section]||"—":"—",location:location>=0?r[location]||"Resumo municipal":"Resumo municipal",municipality:r[city],eligible:eligible>=0?Number((r[eligible]||"0").replace(/\D/g,""))||0:0,votes:Number((r[votes]||"0").replace(/\./g,"").replace(",","."))||0}))}
export async function GET(){const url=process.env.SHEET_CSV_URL;if(!url)return NextResponse.json({rows:[],source:"demo",updatedAt:new Date().toISOString(),message:"Configure SHEET_CSV_URL"});try{const r=await fetch(url,{cache:"no-store"});if(!r.ok)throw 0;return NextResponse.json({rows:parseCSV(await r.text()),source:"sheet",updatedAt:new Date().toISOString()})}catch{return NextResponse.json({rows:[],source:"demo",updatedAt:new Date().toISOString(),message:"Planilha indisponível"})}}
