import {NextResponse} from "next/server";
function parseCSV(text:string){const lines=text.replace(/^\uFEFF/,"").trim().split(/\r?\n/),split=(l:string)=>l.split(/,(?=(?:[^"]*"[^"]*")*[^"]*$)/).map(v=>v.replace(/^"|"$/g,"").trim()),rows=lines.map(split),header=(rows.shift()||[]).map(v=>v.toLocaleLowerCase("pt-BR")),find=(...n:string[])=>header.findIndex(h=>n.some(x=>h.includes(x))),section=find("seção","secao"),location=find("local"),city=find("município","municipio","cidade"),eligible=find("aptos"),candidate=header.findIndex(h=>h.includes("sérgio")||h.includes("sergio")),votes=candidate>=0?candidate:Number(process.env.SHEET_VOTES_COLUMN_INDEX||"4");return rows.filter(r=>r[section]).map(r=>({section:r[section]||"—",location:r[location]||"Local não informado",municipality:city>=0&&r[city]?r[city]:"Camocim",eligible:Number((r[eligible]||"0").replace(/\D/g,""))||0,votes:Number((r[votes]||"0").replace(/\./g,"").replace(",","."))||0}))}
export async function GET(){const url=process.env.SHEET_CSV_URL;if(!url)return NextResponse.json({rows:[],source:"demo",updatedAt:new Date().toISOString(),message:"Configure SHEET_CSV_URL"});try{const r=await fetch(url,{cache:"no-store"});if(!r.ok)throw 0;return NextResponse.json({rows:parseCSV(await r.text()),source:"sheet",updatedAt:new Date().toISOString()})}catch{return NextResponse.json({rows:[],source:"demo",updatedAt:new Date().toISOString(),message:"Planilha indisponível"})}}
