"use client";
import {useEffect,useMemo,useState} from "react";
type Row={municipality:string;votes:number;ibgeCode?:string};type Feature={properties:{codarea:string};geometry:{type:"Polygon"|"MultiPolygon";coordinates:any}};const W=520,H=570,P=18;
function rings(f:Feature):number[][][]{return f.geometry.type==="Polygon"?f.geometry.coordinates:f.geometry.coordinates.flat()}
export default function CearaMap({rows,selected,onSelect}:{rows:Row[];selected:string;onSelect:(city:string)=>void}){const[features,setFeatures]=useState<Feature[]>([]),[hover,setHover]=useState<string|null>(null);useEffect(()=>{fetch("/ceara-municipios.geojson").then(r=>r.json()).then(g=>setFeatures(g.features)).catch(()=>setFeatures([]))},[]);const byCode=useMemo(()=>{const m=new Map<string,{name:string;votes:number}>();for(const r of rows){if(!r.ibgeCode)continue;const old=m.get(r.ibgeCode);m.set(r.ibgeCode,{name:r.municipality,votes:(old?.votes||0)+r.votes})}return m},[rows]);const bounds=useMemo(()=>{const pts=features.flatMap(f=>rings(f).flat());if(!pts.length)return[-41.5,-7.9,-37.1,-2.7];return[Math.min(...pts.map(p=>p[0])),Math.min(...pts.map(p=>p[1])),Math.max(...pts.map(p=>p[0])),Math.max(...pts.map(p=>p[1]))]},[features]);const path=(f:Feature)=>rings(f).map(r=>r.map(([x,y],i)=>{const px=P+(x-bounds[0])/(bounds[2]-bounds[0])*(W-P*2),py=H-P-(y-bounds[1])/(bounds[3]-bounds[1])*(H-P*2);return`${i?"L":"M"}${px.toFixed(1)},${py.toFixed(1)}`}).join("")+"Z").join("");const max=Math.max(1,...Array.from(byCode.values()).map(x=>x.votes)),active=hover?byCode.get(hover):null;const color=(code:string)=>{const item=byCode.get(code),ratio=(item?.votes||0)/max;if(item?.name===selected)return"#ffb020";if(!ratio)return"#0b6689";return ratio>.66?"#18d8d2":ratio>.33?"#1ca0aa":"#197998"};return <div className="real-map"><svg viewBox={`0 0 ${W} ${H}`} role="img" aria-label="Mapa dos 184 municípios do Ceará">{features.map(f=>{const item=byCode.get(f.properties.codarea);return <path key={f.properties.codarea} d={path(f)} fill={color(f.properties.codarea)} className="municipality" onMouseEnter={()=>setHover(f.properties.codarea)} onMouseLeave={()=>setHover(null)} onClick={()=>item&&onSelect(item.name)}><title>{item?.name||`Município IBGE ${f.properties.codarea}`}: {item?.votes||0} votos</title></path>})}</svg>{active&&<div className="map-tooltip"><b>{active.name}</b><span>{active.votes.toLocaleString("pt-BR")} votos</span></div>}<div className="map-scale"><span>0</span><i/><span>{max.toLocaleString("pt-BR")} votos</span></div></div>}
